{
  "dependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx}": [
      "npx prettier --write ."
    ],
    "*.{json,md,css,scss}": [
      "npx prettier --write ."
    ]
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "prettier": "^3.6.2"
  }
}
